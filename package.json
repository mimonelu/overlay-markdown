{
  "name": "overlay-markdown",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel index.html --dist-dir .parcel-local --open --no-source-maps",
    "build": "parcel build index.html --dist-dir docs --public-url /overlay-markdown/ --no-content-hash --no-source-maps"
  },
  "author": "mimonelu",
  "license": "ISC",
  "dependencies": {
    "marked": "^13.0.0"
  }
}
